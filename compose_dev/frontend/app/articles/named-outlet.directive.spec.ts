import { NamedOutletDirective } from './named-outlet.directive';

describe('NamedOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new NamedOutletDirective();
    expect(directive).toBeTruthy();
  });
});
